# 🛠️ 数组与模拟 (Array & Simulation) 学习笔记

## 1. 双指针法 (Two Pointers)
双指针是处理数组问题最常用的技巧之一，通常可以大幅降低时间复杂度。

### 核心应用场景
- **快慢指针**：用于移动元素、删除重复项（如 283. 移动零）。
- **对撞指针**：用于反转字符串、寻找两数之和。

### 实战：移动零 [283]
**思路**：`fast` 遍历，`slow` 记录下一个非零放置位置。
```typescript
function moveZeroes(nums: number[]): void {
    let slow = 0;
    for (let fast = 0; fast < nums.length; fast++) {
        if (nums[fast] !== 0) {
            // 通过交换，保证非零元素移到前面，且原位置自动补零
            [nums[slow], nums[fast]] = [nums[fast], nums[slow]];
            slow++;
        }
    }
}
```

---

## 2. 矩阵旋转与模拟 [1861]
旋转盒子结合了“重力模拟”与“矩阵旋转”。

### 解题核心
1. **重力模拟**（行内位移）：从后往前遍历，维护一个 `k` 指针指向最右侧可放置的位置。
2. **矩阵旋转 90 度**：
   - 坐标映射公式：`newGrid[j][m - 1 - i] = oldGrid[i][j]`
   - 其中 `m` 是原矩阵的行数。

---

## 3. 已练习题目汇总
| 题号 | 题目 | 难点 |
| :--- | :--- | :--- |
| [283] | 移动零 | 如何保证在 $O(1)$ 空间内完成，且操作次数最少。 |
| [1861] | 旋转盒子 | 模拟重力落点时需要处理障碍物 `*` 带来的位置重置。 |
